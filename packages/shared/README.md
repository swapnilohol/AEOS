# packages/shared

Shared types and utilities used by both `apps/frontend` and `apps/backend`.

Empty at Bootstrap. Populated as modules are implemented (e.g. shared
TypeScript interfaces generated from backend Pydantic schemas).
